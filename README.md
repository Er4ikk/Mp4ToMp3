Simple file converter from mp4 video format to mp3/wav.
All written in .NET using Avalonia for the UI

to build:
dotnet build

to release and execute:
dotnet publish -r <RID>

examples:
- dotnet publish -r linux-x64
- dotnet publish -r winx-64
